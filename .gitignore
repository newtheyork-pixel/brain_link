node_modules/
voices/cache/
voices/*.onnx
data/sessions.jsonl
.DS_Store

# whisper.cpp build output + model (148MB). Rebuild with: ./asr/setup
asr/whisper-cli
asr/*.dylib
asr/*.bin
.gstack/
.env
public/vendor/*.wasm
public/vendor/*.task

# Per-user data — a real family's profile, utterances, and gaze traces never touch git.
data/profile.json
data/*.jsonl
!data/*.example.json
